<div class="page-header">
  <h1>Companies</h1>
</div>

<% if @companies.present? %>
  <table class="table">
    <thead>
      <tr>
        <th>Name</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @companies.each do |company| %>
        <tr>
          <td><%= company.name %></td>
          <td>
            <%= link_to "Company Events", company_events_path(company) %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <div class="jumbotron">
    <h1>You forgot to run <code>rake db:seed</code></h1>
  </div>
<% end %>
